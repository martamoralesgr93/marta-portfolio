# Sin esta variable el formulario abre el gestor de correo con la consulta
# ya redactada (mailto:). Con ella, hace POST del JSON al endpoint indicado
# —Formspree, Resend, una función serverless propia…— y muestra la
# confirmación dentro de la página.
VITE_CONTACT_ENDPOINT=
